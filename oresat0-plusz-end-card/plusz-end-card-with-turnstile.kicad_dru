(version 1)
(rule "class 1:1"
  (condition "A.NetClass == 'RF' && B.NetClass == 'RF'")
  (constraint clearance (min 0.85mm)))

(rule "class 2:2"
  (condition "A.NetClass == 'SHUTDOWN' && B.NetClass == 'SHUTDOWN'")
  (constraint clearance (min 0.3mm)))
